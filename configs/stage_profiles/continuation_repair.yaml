{
  "name": "continuation_repair",
  "strictness": "strict",
  "eval_pack": "continuation_repair",
  "recipe_template": "repair_continuation",
  "init_mode": "continue_from_checkpoint",
  "checkpoint_mode": "save_interval",
  "deterministic_gates": {
    "max_toxicity": 0.05,
    "min_probe_score": 0.75
  },
  "allowed_next_actions": [
    "promote_checkpoint",
    "rollback_to_checkpoint",
    "abort_run"
  ],
  "certification_profile": {
    "eligibility": "strict",
    "require_recheck": true,
    "min_consistent_runs": 2,
    "repeatability_required": true,
    "required_rechecks": 2,
    "min_repeat_consistency": 0.8,
    "variance_sensitivity": "high",
    "certification_recheck_policy": "required_if_repeatability_unmet"
  }
}
