{
  "name": "scale_up_pretraining",
  "strictness": "lenient",
  "eval_pack": "pretraining_probes",
  "recipe_template": "pretrain_continue",
  "init_mode": "continue_from_checkpoint",
  "checkpoint_mode": "save_interval",
  "deterministic_gates": {
    "max_toxicity": 0.18,
    "min_probe_score": 0.6
  },
  "allowed_next_actions": [
    "continue_lineage_best",
    "rerun_same_config",
    "reject_checkpoint"
  ],
  "certification_profile": {
    "eligibility": "bounded",
    "require_recheck": false,
    "min_consistent_runs": 1,
    "repeatability_required": false,
    "required_rechecks": 1,
    "min_repeat_consistency": 0.6,
    "variance_sensitivity": "medium",
    "certification_recheck_policy": "required_if_repeatability_unmet"
  }
}
